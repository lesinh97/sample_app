<%= link_to gravatar_for(current_user, size: Settings.picture_size_index), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("link_owner_profile"), current_user %></span>
<span><%= pluralize current_user.microposts.count, Settings.micropost_label %></span>
